# Media Failure Pack

These rootless profiles make HLS segments disappear, replace live manifests with stale playlists, oscillate available bitrate, and delay WebRTC signaling. Point the player or signaling client at each scenario's local `listen` address. RTP-aware keyframe loss remains planned because a generic byte proxy cannot identify codec frame boundaries honestly.
