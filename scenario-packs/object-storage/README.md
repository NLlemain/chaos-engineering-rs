# Object Storage Failure Pack

These HTTP and TCP profiles model partial uploads, stale object reads, throttling, and deterministic corrupt downloads for S3-compatible, Azure Blob, GCS-compatible, MinIO, and local object stores. Replace the example upstream and paths with disposable test resources.
