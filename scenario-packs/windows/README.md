# Windows Failure Pack

These Windows-only profiles stop and restore a service, hold an exclusive file lock, create reversible handle pressure, or expose a named pipe that accepts a client and never responds. Service outage requires an elevated terminal; the other modes are rootless. Run them against disposable test targets and keep the recovery journal enabled.
